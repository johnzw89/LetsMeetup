<%= form_for(@back_pocket) do |f| %>
  <% if @back_pocket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@back_pocket.errors.count, "error") %> prohibited this back_pocket from being saved:</h2>

      <ul>
      <% @back_pocket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
